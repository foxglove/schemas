// Generated from PointCloud by @foxglove/message-schemas

syntax = "proto3";

import "foxglove/DataField.proto";
import "foxglove/Pose.proto";
import "foxglove/Time.proto";

package foxglove;

// A collection of N-dimensional points, which may contain additional fields with information like normals, intensity, etc.
message PointCloud {
  // Timestamp of point cloud
  foxglove.Time timestamp = 1;

  // Frame of reference
  string frame_id = 2;

  // The origin of the point cloud relative to the frame of reference
  foxglove.Pose pose = 3;

  // Number of bytes between points in the `data`
  int32 point_stride = 4;

  // Fields in the `data`
  foxglove.DataField fields = 5;

  // Point data, interpreted using `fields`
  bytes data = 6;
}
