// Generated by https://github.com/foxglove/schemas

syntax = "proto3";

import "foxglove/ArrowMarker.proto";
import "foxglove/ConeMarker.proto";
import "foxglove/CubeMarker.proto";
import "foxglove/KeyValuePair.proto";
import "foxglove/LineMarker.proto";
import "foxglove/ModelMarker.proto";
import "foxglove/SphereMarker.proto";
import "foxglove/TextMarker.proto";
import "foxglove/TriangleListMarker.proto";
import "google/protobuf/duration.proto";
import "google/protobuf/timestamp.proto";

package foxglove;

// A visual element in a 3D scene
message SceneEntity {
  // Timestamp of the marker
  google.protobuf.Timestamp timestamp = 1;

  // Frame of reference
  string frame_id = 2;

  // Identifier for the marker. A marker will replace any prior marker on the same topic with the same `id`.
  string id = 3;

  // Length of time (relative to `timestamp`) after which the marker should be automatically removed. Zero value indicates the marker should remain visible until it is replaced or deleted.
  google.protobuf.Duration lifetime = 4;

  // Whether the marker should keep its location in the fixed frame (false) or follow the frame specified in `frame_id` as it moves relative to the fixed frame (true)
  bool frame_locked = 5;

  // Additional user-provided metadata associated with the marker. Keys must be unique.
  repeated foxglove.KeyValuePair metadata = 6;

  // Arrow markers
  repeated foxglove.ArrowMarker arrows = 7;

  // Cube list markers
  repeated foxglove.CubeMarker cubes = 8;

  // Sphere list markers
  repeated foxglove.SphereMarker spheres = 9;

  // Cone list markers
  repeated foxglove.ConeMarker cones = 10;

  // Line markers
  repeated foxglove.LineMarker lines = 11;

  // Triangle list markers
  repeated foxglove.TriangleListMarker triangles = 12;

  // Text markers
  repeated foxglove.TextMarker texts = 13;

  // Model markers
  repeated foxglove.ModelMarker models = 14;
}
