// Generated from Markers by @foxglove/schemas

syntax = "proto3";

import "foxglove/ArrowMarker.proto";
import "foxglove/CubeMarker.proto";
import "foxglove/CylinderMarker.proto";
import "foxglove/LineMarker.proto";
import "foxglove/MarkerDeletion.proto";
import "foxglove/ModelMarker.proto";
import "foxglove/SphereMarker.proto";
import "foxglove/TextMarker.proto";
import "foxglove/TrianglesMarker.proto";

package foxglove;

// A list that can contain any number of any type of marker
message Markers {
  // Marker deletion actions
  repeated foxglove.MarkerDeletion deletions = 1;

  // Arrow markers
  repeated foxglove.ArrowMarker arrows = 2;

  // Cube markers
  repeated foxglove.CubeMarker cubes = 3;

  // Sphere markers
  repeated foxglove.SphereMarker spheres = 4;

  // Cylinder markers
  repeated foxglove.CylinderMarker cylinders = 5;

  // Line markers
  repeated foxglove.LineMarker lines = 6;

  // Triangles markers
  repeated foxglove.TrianglesMarker triangles = 7;

  // Text markers
  repeated foxglove.TextMarker texts = 8;

  // Model markers
  repeated foxglove.ModelMarker models = 9;
}
