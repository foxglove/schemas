// Generated by https://github.com/foxglove/schemas

syntax = "proto3";

import "foxglove/ArrowMarker.proto";
import "foxglove/ConeListMarker.proto";
import "foxglove/CubeListMarker.proto";
import "foxglove/LineMarker.proto";
import "foxglove/MarkerDeletion.proto";
import "foxglove/ModelMarker.proto";
import "foxglove/SphereListMarker.proto";
import "foxglove/TextMarker.proto";
import "foxglove/TriangleListMarker.proto";

package foxglove;

// A list of any number or type of markers
message Markers {
  // Marker deletion actions
  repeated foxglove.MarkerDeletion deletions = 1;

  // Arrow markers
  repeated foxglove.ArrowMarker arrows = 2;

  // Cube list markers
  repeated foxglove.CubeListMarker cubes = 3;

  // Sphere list markers
  repeated foxglove.SphereListMarker spheres = 4;

  // Cone list markers
  repeated foxglove.ConeListMarker cones = 5;

  // Line markers
  repeated foxglove.LineMarker lines = 6;

  // Triangle list markers
  repeated foxglove.TriangleListMarker triangles = 7;

  // Text markers
  repeated foxglove.TextMarker texts = 8;

  // Model markers
  repeated foxglove.ModelMarker models = 9;
}
